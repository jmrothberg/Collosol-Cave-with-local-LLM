# Environment Variables for Colossal Cave Adventure
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# Hugging Face Token (required for downloading certain models)
# Get your token from: https://huggingface.co/settings/tokens
HF_TOKEN=your_huggingface_token_here

# Optional: Ollama API key (only if using remote Ollama server)
# OLLAMA_API_KEY=your_ollama_api_key_here

# Optional: Custom paths for diffusion models
# DIFFUSION_MODELS_DIR=/path/to/your/Diffusion_Models

# Optional: Select specific GPU for diffusion (0, 1, 2, etc.)
# DIFFUSION_GPU=0
